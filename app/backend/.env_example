# Database configuration
# For development (SQLite)
DATABASE_URL=sqlite:///./data/polypore.db
# For production (PostgreSQL) - example:
# DATABASE_URL=postgresql://username:password@db-server:5432/polypore_db

# CORS configuration (comma-separated list of allowed origins)
# For development (local frontend)
BACKEND_CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
# For production (replace with your actual frontend domain):
# BACKEND_CORS_ORIGINS=https://your-frontend-domain.com,https://www.your-frontend-domain.com